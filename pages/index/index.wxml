<view class="container">
  <!-- 顶部标题 -->
  <view class="header">
    <view class="title-row">
      <image class="logo" src="/images/logo.png" mode="aspectFit"></image>
      <view class="title">Italiano单词宝</view>
    </view>
    <text class="subtitle">每天5分钟，轻松学意大利语</text>
  </view>

  <!-- 级别选择区域 -->
  <view class="levels-area">
    <view class="level-card" wx:for="{{levels}}" wx:key="level" bindtap="startLevelStudy" data-level="{{item.level}}">
      <view class="level-badge level-{{item.level}}">{{item.level}}</view>
      <view class="level-content">
        <view class="progress-bar-container">
          <view class="progress-bar progress-{{item.level}}" style="width: {{item.progress}}%"></view>
        </view>
        <view class="level-stats">{{item.learned}}/{{item.total}}</view>
      </view>
    </view>
  </view>

  <!-- 底部功能区 -->
  <view class="footer">
    <view class="footer-actions">
      <view class="footer-action-btn" bindtap="goToMistakes">
        <text class="footer-icon">📚</text>
        <text class="footer-label">错题本</text>
      </view>
      <view class="footer-action-btn" bindtap="goToFavorites">
        <text class="footer-icon">⭐</text>
        <text class="footer-label">收藏</text>
      </view>
    </view>
        <button class="debug-btn" bindtap="initDatabase">初始化云数据</button>
  </view>
</view>
